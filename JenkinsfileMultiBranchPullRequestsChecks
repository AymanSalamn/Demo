pipeline {
    agent any

    stages {
        stage('Get Changed Files') {
            steps {
                script {
                  sh "git branch -r"
                }
            }
        }
    }
}
