pipeline {
    agent any

    stages {
        stage('Get Changed Files') {
            steps {
                script {
                    sh "git --no-pager diff origin/main --name-only"
                }
            }
        }
    }
}
