pipeline {
    agent any
    stages {
        stage('Run Test') {
            steps {
                script {
                    sh "mkdir testing"
                }
            }
        }
    }
    
    // Add post-build actions if necessary
    post {
        always {
            // Always clean the workspace regardless of the build result
            deleteDir()
        }
    }
}
