pipeline {
    agent any

    stages {
        stage('Get Affected Files') {
            steps {
                script {
                    def changedFiles = pullRequest.files.collect {
                        it.getFilename()
                    }
                    sh "echo ${changedFiles}"
                    
                }
            }
        }
    }
}
