pipeline {
    agent any
    
    stages {
        stage('Install Allure') {
            steps {
                script {
                    // GET current dir 
                    def current_dir = pwd()
                    sh 'echo ${current_dir}'
                }
            }
        }
        
        // Add other stages of your pipeline
    }
    
    // Add post-build actions if necessary
}
