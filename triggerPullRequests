pipeline {
    agent any

    stages {
        stage('Run Tests') {
            steps {
                script {
                    sh "pytest" 
                }
            }
        }
    }

    post {
            always {
                // Always clean the workspace regardless of the build result
                deleteDir()
            }
        }
}
