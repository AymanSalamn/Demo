pipeline {
    agent any
    
    environment {
        PATH = "/var/lib/jenkins/.local/bin:$PATH"
    }

    stages {
        stage('Update Build Name & Description') {
            steps {
                script {
                    sh "pytest --version"
                    sh "python3 -m pytest --version"
                }
            }
        }
    }
    
    post {
        always {
            deleteDir()
        }
    }
}
