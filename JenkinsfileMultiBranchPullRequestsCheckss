pipeline {
    agent any
    
    stages {
        stage('Test') {
            steps {
                // Your test steps
                echo "Testing"
            }
        }
    }
    
    post {
        success {
            script {
                githubCommitStatus(
                    context: 'Jenkins',
                    state: 'success',
                    description: 'Custom message for successful build!'
                )
            }
        }
        failure {
            script {
                githubCommitStatus(
                    context: 'Jenkins',
                    state: 'failure',
                    description: 'Custom message for failed build!'
                )
            }
        }
    }
}
