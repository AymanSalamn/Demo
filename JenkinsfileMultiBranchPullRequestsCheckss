pipeline {
    agent any
    stages {
        stage('Update Build Name & Description') {
            steps {
                script {
                    sh "/usr/bin/python3 --version"
                }
            }
        }
    }
    
    post {
        always {
            deleteDir()
        }
    }
}
