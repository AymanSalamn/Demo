pipeline {
    agent any
    stages {
        stage('Update Build Name & Description') {
            steps {
                script {
                    sh "pip install pytest==8.1.1"
                    sh "pytest --version"
                }
            }
        }
    }
    
    post {
        always {
            deleteDir()
        }
    }
}
