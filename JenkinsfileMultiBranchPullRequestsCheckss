pipeline {
    agent any
    stages {
        stage('Update Build Name & Description') {
            steps {
                script {
                    sh "/usr/bin/python3 -m pytest --vresion"
                }
            }
        }
    }
    
    post {
        always {
            deleteDir()
        }
    }
}
