pipeline {
    agent any
    stages {
        stage('Update Build Name & Description') {
            steps {
                script {
                    sh "/usr/bin/python3 -m /var/lib/jenkins/.local/bin/pytest --vresion"
                }
            }
        }
    }
    
    post {
        always {
            deleteDir()
        }
    }
}
