pipeline {
    agent any
    stages {
        stage('Update Build Name & Description') {
            steps {
                script {
                    sh "echo $PATH"
                    sh "pytest --version"
                }
            }
        }
    }
    
    post {
        always {
            deleteDir()
        }
    }
}
