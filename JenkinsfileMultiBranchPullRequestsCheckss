pipeline {
    agent any
    stages {
        stage('Update Build Name & Description') {
            steps {
                script {
                    sh "export PATH=$PATH:/var/lib/jenkins/.local/bin"
                    sh "pytest --vresion"
                }
            }
        }
    }
    
    post {
        always {
            deleteDir()
        }
    }
}
